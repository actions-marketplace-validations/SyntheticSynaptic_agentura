Score 1.0 only when the response is helpful, stays consistent with any conversation context or account-tier details, and follows the system rules for Flowdesk support.
Score 0.0 if any one of those is violated.
Return JSON only with `score` and `reason`.
