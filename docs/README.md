# Docs Assets

GIF pending first run.

To regenerate the demo GIF: brew install vhs && vhs demo.tape

Run that command from `docs/`. The tape records the secret-free `examples/demo-agent` flow so it works without provider keys.
